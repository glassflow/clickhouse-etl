---
title: 'Supported Kafka Connections'
description: 'Supported Kafka connection protocols, authentication, and TLS options'
---
import { Callout, Tabs } from 'nextra/components'

# Supported Kafka Connections

GlassFlow supports connecting to Kafka clusters with multiple protocols and authentication methods. This page lists the supported options and how to configure them.

<Callout type="info" emoji="ℹ️">
Configuration examples below refer to the pipeline source connection parameters. You can configure these via the Web UI or API.
</Callout>

## Connection Protocols

Set the **Security Protocol** ([`protocol`](/configuration/pipeline-json-reference#kafka-connection-parameters)) according to your broker configuration:

- **PLAINTEXT**
- **SASL_PLAINTEXT**
- **SSL**
- **SASL_SSL**

## SASL Authentication

Set one of the following **Authentication Methods** ([`mechanism`](/configuration/pipeline-json-reference#kafka-connection-parameters)):

- **NO_AUTH**
- **PLAIN**
- **SCRAM-SHA-256**
- **SCRAM-SHA-512**
- **GSSAPI**

One can disable authentication entirely by using the mechanism `NO_AUTH` (On the UI is called "No authentication"). It is also possible to skip TLS verification by using the `skip_tls_verification` flag.

When authentication is enabled, the following fields are required:
  - `mechanism` (one of the above)
  - `username`
  - `password`

<Callout type="warning" emoji="⚠️">
For **GSSAPI** (Kerberos) authentication, different fields are required. See the [SASL/GSSAPI (Kerberos)](#saslgssapi-kerberos) section below for details.
</Callout>

## TLS Configuration

Enable TLS via protocol SSL/SASL_SSL and optionally supply root_ca to trust a custom CA.

Client certificate authentication (mTLS) is not supported.

## Example Configurations

### PLAINTEXT (no auth)

<Tabs items={['Web UI', 'Python SDK']} groupId="kafka-conn" storageKey="kafka-conn-tab">
  <Tabs.Tab>
    <img alt="PLAINTEXT (no auth) example" src="/assets/ui_kafka_connection_plaintext_no_auth.png" />
  </Tabs.Tab>
  <Tabs.Tab>
    ```json
    "connection_params": {
      "brokers": ["kafka-1:9092"],
      "protocol": "PLAINTEXT",
      "mechanism": "NO_AUTH",
    }
    ```
  </Tabs.Tab>
</Tabs>

### SASL/PLAIN over TLS

<Tabs items={['Web UI', 'Python SDK']} groupId="kafka-conn" storageKey="kafka-conn-tab">
  <Tabs.Tab>
    <img alt="SASL/PLAIN over TLS example" src="/assets/ui_kafka_connection_sasl_plain_over_tls.png" />
  </Tabs.Tab>
  <Tabs.Tab>
    ```json
    "connection_params": {
      "brokers": ["kafka-1:9093"],
      "protocol": "SASL_SSL",
      "mechanism": "PLAIN",
      "username": "user",
      "password": "pass",
      "root_ca": "-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----\n"
    }
    ```
  </Tabs.Tab>
</Tabs>

### SASL/SCRAM-SHA-256

<Tabs items={['Web UI', 'Python SDK']} groupId="kafka-conn" storageKey="kafka-conn-tab">
  <Tabs.Tab>
    <img alt="SASL/SCRAM-SHA-256 example" src="/assets/ui_kafka_connection_sasl_scram_256.png" />
  </Tabs.Tab>
  <Tabs.Tab>
    ```json
    "connection_params": {
      "brokers": ["kafka-1:9093"],
      "protocol": "SASL_PLAINTEXT",
      "mechanism": "SCRAM-SHA-256",
      "username": "scram-user",
      "password": "scram-password"
    }
    ```
  </Tabs.Tab>
</Tabs>

### SASL/GSSAPI (Kerberos)

GlassFlow supports Kerberos authentication (GSSAPI) for connecting to Kafka clusters secured with Kerberos. This is commonly used in enterprise environments.

**Required fields for GSSAPI:**
  - `mechanism`: `"GSSAPI"`
  - `username`: Kerberos principal (e.g., `"user@EXAMPLE.COM"`)
  - `kerberos_service_name`: Service name (typically `"kafka"`)
  - `kerberos_realm`: Kerberos realm (e.g., `"EXAMPLE.COM"`)
  - `kerberos_keytab`: Base64-encoded keytab file content
  - `kerberos_config`: Kerberos configuration (krb5.conf format)

**Optional fields:**
  - `password`: Required only if using password-based authentication instead of keytab

<Tabs items={['Web UI', 'Python SDK']} groupId="kafka-conn" storageKey="kafka-conn-tab">
  <Tabs.Tab>
    <img alt="SASL/GSSAPI (Kerberos) example" src="/assets/ui_kafka_connection_sasl_gssapi.png" />
  </Tabs.Tab>
  <Tabs.Tab>
    ```json
    "connection_params": {
      "brokers": ["kafka-1:9093"],
      "protocol": "SASL_SSL",
      "mechanism": "GSSAPI",
      "username": "kafka-user@EXAMPLE.COM",
      "kerberos_service_name": "kafka",
      "kerberos_realm": "EXAMPLE.COM",
      "kerberos_keytab": "BQIAAABH...",
      "kerberos_config": "[libdefaults]\n\tdefault_realm = EXAMPLE.COM\n\n[realms]\n\tEXAMPLE.COM = {\n\t\tkdc = kdc-server.example.com\n\t\tadmin_server = kdc-server.example.com\n\t}",
      "root_ca": "-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----\n"
    }
    ```
  </Tabs.Tab>
</Tabs>

<Callout type="info" emoji="ℹ️">
The `kerberos_keytab` field should contain the base64-encoded content of your keytab file. The `kerberos_config` field should contain the contents of your krb5.conf file, which typically includes realm and KDC (Key Distribution Center) configuration.
</Callout>

### SSL (skip TLS verification)

<Tabs items={['Web UI', 'Python SDK']} groupId="kafka-conn" storageKey="kafka-conn-tab">
  <Tabs.Tab>
    <img alt="SSL (skip TLS verification) example" src="/assets/ui_kafka_connection_ssl_skip_tls_verification.png" />
  </Tabs.Tab>
  <Tabs.Tab>
    ```json
    "connection_params": {
      "brokers": ["kafka-1:9093"],
      "protocol": "SSL",
      "mechanism": "PLAIN",
      "skip_tls_verification": true
    }
    ```
  </Tabs.Tab>
</Tabs>
