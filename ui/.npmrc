# Enable build scripts for native addons like node-rdkafka
# By default, pnpm ignores scripts for security reasons
# but we need them for native compilation
enable-pre-post-scripts=true
ignore-scripts=false

