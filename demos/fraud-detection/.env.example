# Credentials for `glassflow up --demo` (Kafka SCRAM + ClickHouse). Safe to commit; rotate in real deployments.
KAFKA_USERNAME=user1
KAFKA_PASSWORD=glassflow-demo-password
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=glassflow-demo-password
GLASSFLOW_API=http://localhost:30180
